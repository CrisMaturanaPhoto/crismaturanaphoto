---
import Layout from '../layouts/Layout.astro';
import HomeContent from '../components/HomeContent.astro';
import { heroSlides, portfolioCategories } from '../lib/ui';
import { getLatestBlogPosts, buildAlternates } from '../lib/utils';
import SpecialOfferPopup from '../components/SpecialOfferPopup.astro';

const latestBlogPosts = await getLatestBlogPosts(3);
const alternates = buildAlternates('/', '/es/');
---

<Layout
  title="Cristóbal Maturana – Documentary Photographer"
  lang="en"
  alternates={alternates}
>
  <HomeContent
    heroSlides={heroSlides}
    portfolioCategories={portfolioCategories}
    latestBlogPosts={latestBlogPosts}
  />
  <SpecialOfferPopup />
</Layout>
